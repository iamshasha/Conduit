# Rust
/target/

# WinUI / .NET
gui-winui/bin/
gui-winui/obj/
gui/bin/
gui/obj/

# Publish output
**/publish/
*.user

# OS / editor
Thumbs.db
.DS_Store
.vs/

# GTK GUI build
gui-gtk/target/

# Packaged installers (built by CI / packaging scripts)
/dist/

# Icon PNGs are rendered from assets/conduit.svg by the packaging scripts.
assets/conduit-*.png

# Swift build output (the real Strings.json is copied in from gui-winui at build
# time, overwriting the committed placeholder).
packaging/macos/ConduitGUI/.build/
